@model List<WebApp.Models.DilOkulu_KonaklamaTipiFiyatlandirmalari>
@{
    Dictionary<string, decimal?> list = new Dictionary<string, decimal?>();
    if (Model != null && Model.Count > 0)
    {
        for (int i = 0; i < Model.Count; i++)
        {
            list.Add(Model[i].HerHaftayaOzel_Hafta.ToString(), Model[i].Fiyat);
        }
    }
}
<table cellpadding="0" cellspacing="0" id="haftayaOzel">
    <tr><td class="baslik">Haftalar</td><td class="baslik">Fiyat</td><td></tr>
    @for (int i = 1; i <= 36; i++)
    {
        <tr>
            <td class="baslik">@(i). Hafta</td>
            <td>
                <input type="text" class="TextBox haftayaOzelInput" data-hafta="@(i)" value="@(list.Keys.Contains(i.ToString())?list.First(k=>k.Key==i.ToString()).Value.ToString():"")" style="width:40px;" />
            </td>
        </tr>
    }
    <tr><td colspan="2" align="right"><input type="button" id="btnHaftayaOzel_Ekle" data-fiyatlandirmatipiid="2" value="Kaydet" /></td></tr>
</table>
<hr />
<b>Nasıl Kullanılır?</b><br />
Bu fiyatlandırma modeli fiyatları arasında orantısal bir ilişki olmayan konaklama tipleri kullanılmalıdır. 1 haftadan 52 haftaya kadar her hafta için fiyat girilmelidir. 